# GeoCore Academy — Roadmap

## В разработке / Запланировано

### AI-чат ассистент (Claude API)
**Приоритет:** высокий  
**Статус:** идея, не начато

Заменить офлайн-бот (`frontend/js/ui/chat.js`) на реальный AI-ассистент на базе Claude API.

**Что нужно:**
- Эндпоинт `POST /api/chat` в `backend/main.py` — принимает сообщение, возвращает ответ Claude
- `ANTHROPIC_API_KEY` в `.env` на VPS
- Системный промпт: геолог-консультант по Datamine, Leapfrog, JORC/ГКЗ, геостатистике
- Фронтенд: `chat.js` делает fetch на `/api/chat` вместо офлайн-паттернов
- Стриминг ответа (опционально — для ощущения живого чата)

**Ценность:** реальная помощь геологам прямо на сайте, конверсия в заявки на курсы.

---

### Ссылка «Статьи для специалистов» в хэдере
**Приоритет:** средний  
**Статус:** идея, не начато

Добавить пункт в главное меню сайта, ведущий на секцию `#blog`.
Уточнить у владельца: переименовать «Блог» → «Статьи» или добавить отдельно.

---

### Саморегистрация студентов в Moodle
**Приоритет:** низкий  
**Статус:** не начато

Настроить самостоятельную запись на курсы без участия администратора.

---

### Разделение бэкенда
**Приоритет:** низкий  
**Статус:** не начато

- **VPS API** — курсы, заявки, admin, AI-чат
- **Локальный API** — тяжёлые геологические расчёты (`processing/`)

---

### UptimeRobot (внешний мониторинг)
**Приоритет:** низкий  
**Статус:** не начато

Внешний пинг трёх доменов + Telegram-алерт если VPS упал.
