# ============================================================
# GeoCore Academy — Production Environment Variables
# Скопировать в .env и заполнить реальными значениями:
#   cp .env.example .env
# ============================================================

# Docker Hub (имя пользователя, куда пушим образы)
DOCKERHUB_USER=andreyhiitola

# Версия Moodle (должна совпадать с тегом образа на Docker Hub)
# Для обновления: 1) протестировать в docker-compose.test.yml
#                 2) изменить здесь + пересобрать образ
MOODLE_VERSION=5.1.3

# --- База данных ---
DB_ROOT_PASSWORD=change_me_strong_root_password
MOODLE_DB_NAME=moodle
MOODLE_DB_USER=moodle
MOODLE_DB_PASSWORD=change_me_strong_db_password

# --- Moodle настройки ---
MOODLE_WWWROOT=https://courses.geocore-academy.ru
MOODLE_SITE_NAME=GeoCore Academy
MOODLE_SITE_SHORTNAME=geocore
MOODLE_LANG=ru

# --- Moodle администратор ---
# Пароль должен содержать: буквы + цифры, минимум 8 символов
MOODLE_ADMIN_USER=admin
MOODLE_ADMIN_PASS=change_me_Admin1234
MOODLE_ADMIN_EMAIL=admin@geocore-academy.ru

# --- Backend API ---
API_SECRET_KEY=change_me_random_secret_key_32chars
# Публичные CSV endpoints (/api/validate, /api/process, /api/wireframe, /api/mac)
MAX_CSV_BYTES=5242880
MAX_CSV_ROWS=20000

# --- Moodle Web Service Token ---
# Получить в Moodle: Сервер → Веб-службы → Управление ключами
# Нужны функции: core_user_create_users, enrol_manual_enrol_users, core_course_get_courses
MOODLE_TOKEN=

# --- Реквизиты для счетов на оплату ---
# Подставляются в PDF автоматически при формировании счёта
INVOICE_COMPANY_NAME=          # Полное наименование: ИП Фамилия И.О. или ООО «Название»
INVOICE_INN=                   # ИНН (10 цифр для ООО, 12 для ИП)
INVOICE_KPP=                   # КПП (только для ООО, для ИП оставить пустым)
INVOICE_BANK=                  # Название банка
INVOICE_BIK=                   # БИК банка (9 цифр)
INVOICE_ACCOUNT=               # Расчётный счёт (р/с, 20 цифр)
INVOICE_CORR_ACCOUNT=          # Корреспондентский счёт (к/с, 20 цифр)
INVOICE_ADDRESS=               # Юридический адрес

# --- Email уведомления о заявках ---
# Отправитель: noreply@geocore-academy.ru через Яндекс 360 для бизнеса (бесплатно)
# Инструкция: https://360.yandex.ru/business → подключить домен geocore-academy.ru
# SMTP_PASS: пароль приложения из Яндекс 360 → Безопасность → Пароли приложений
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=noreply@geocore-academy.ru
SMTP_PASS=
NOTIFY_EMAIL=9624294@gmail.com

# --- Бэкапы (Selectel S3) ---
# Создать bucket в Selectel Cloud → Object Storage → зона ru-3
# ACCESS_KEY и SECRET_KEY: Selectel → Профиль → Сервисные пользователи
# Эндпоинт зависит от зоны: ru-3 → s3.ru-3.storage.selcloud.ru
S3_ENDPOINT=https://s3.ru-3.storage.selcloud.ru
S3_BUCKET=geocore-backups
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ru-3

# --- Прокси (если VPS за прокси-шлюзом) ---
# HTTPS_PROXY=http://host:port

# --- Мониторинг (Telegram) ---
# Создать бота: @BotFather → /newbot → получишь токен
# CHAT_ID: написать боту любое сообщение, затем открыть
#   https://api.telegram.org/bot<TOKEN>/getUpdates → найти "chat":{"id":...}
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- AI-ассистент (OpenRouter) ---
# Получить ключ: https://openrouter.ai → Keys → Create Key
# Бесплатные модели: meta-llama/llama-3.3-70b-instruct:free
OPENROUTER_API_KEY=

# --- Админка ---
# Логин и токен для входа в admin.html
ADMIN_USERNAME=admin
ADMIN_TOKEN=change_me_strong_admin_token
# GitHub Personal Access Token с правом repo (Contents: read & write)
# Создать: GitHub → Settings → Developer settings → Personal access tokens → Fine-grained
GITHUB_TOKEN=
GITHUB_REPO=Andreyhiitola/geocore
